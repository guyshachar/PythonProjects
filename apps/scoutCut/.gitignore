# OAuth credentials — never commit these
credentials.json
token.json

# Local pricing overrides — edit to change rates without touching code
web/config.json

# Run artifacts
runs/
temp_clips/

# PyInstaller build output
build/
dist/

# Playwright saved session
veo_profile/
scoutcut.db
